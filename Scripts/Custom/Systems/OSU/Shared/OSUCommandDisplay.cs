using Server;

namespace Server.Custom.OSU
{
    public static class OSUCommandDisplay
    {
        // Prefixo mostrado nos Gumps (ex: "[disarme" ou ".disarme")
        public const string Prefix = "[";
    }
}
